#include <bits/stdc++.h>
using namespace std;
long long a[45e3];
int main()
{
    ifstream fin ("THADIEU.INP");
    fin >> n;
    for (i=1; i<=n; i++) fin >> a[i];
    fin.close();
    ofstream fout ("THADIEU.OUT");
    fout.close();
    return 0;
}
